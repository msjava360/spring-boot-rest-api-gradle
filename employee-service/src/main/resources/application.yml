server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring?useSSL=false
    username: root
    password: root@123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

